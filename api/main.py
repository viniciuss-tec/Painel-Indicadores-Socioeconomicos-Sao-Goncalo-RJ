"""
FAZER INTEGRAÇAO DE BACKEND E DB

O projeto ainda não precisa executar esta API.
Quando PostgreSQL estiver pronto, a interface já terá uma camada data-provider.js para trocar JSON local por API.

Contrato inicial planejado:

GET /api/indicadores
GET /api/indicadores/{codigo}
GET /api/indicadores/{codigo}/serie
GET /api/indicadores/{codigo}/csv
GET /api/temas
"""

# O FastAPI será adicionado na próxima fase.
# Não é necessário instalar fastapi para executar.
